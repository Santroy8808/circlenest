import { ActionGlyph } from "@/components/reactions/action-glyph";

export function ThetaLikeTriangle({ className = "theta-like-triangle" }: { className?: string }) {
  return <ActionGlyph className={className} kind="react" />;
}
